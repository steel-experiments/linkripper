// ABOUTME: PostCSS config wiring Tailwind CSS and autoprefixer.
// ABOUTME: Consumed by Next.js during the CSS build step.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
